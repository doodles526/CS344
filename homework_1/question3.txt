Redirection does one of 2 things.  It can take the stdout of a command and put it in a file, or it can send the text in a file to the stdin of a command.

A pipe simply connects the stdout of a command with the stdin of another, bypassing files completely.
